# =============================================================================
# Molbot - Solo variables necesarias para inicialización del contenedor
# Copiar a .env y ajustar valores. El resto se administra desde la interfaz.
# =============================================================================

# MongoDB - Conexión (imprescindible para arranque)
MONGO_PASSWORD=1nt3r4ct1v3
MONGO_DB=molbot
MONGO_URI=mongodb://root:1nt3r4ct1v3@mongodb:27017/molbot?authSource=admin

# Encriptación - Clave para cifrar tokens/API keys en Mongo (obligatoria si se usan credenciales, mínimo 32 caracteres)
ENCRYPTION_KEY=misuperclavaededencriptacion2026molbot

# UI - Secret para sesión o contraseña de primer acceso (recomendado en producción)
UI_SECRET=1nt3r4ct1v3


# -----------------------------------------------------------------------------
# OpenClaw (formato oficial: https://docs.openclaw.ai/install/docker)
# La imagen openclaw:local se construye desde el repo OpenClaw (ver README o doc/OPENCLAW-DOCKER.md).
# -----------------------------------------------------------------------------
OPENCLAW_IMAGE=openclaw:local
OPENCLAW_CONFIG_DIR=./data/molbot-workspace
OPENCLAW_WORKSPACE_DIR=./data/molbot-workspace/workspace
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan
# Token para conectar al Gateway (Cursor, Control UI). Generar con: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=token_forui
# allowInsecureAuth: solo para desarrollo. En producción, cambiar a false o eliminar esta línea.
OPENCLAW_ALLOW_INSECURE_AUTH=true #Esto se mantiene en true mientras el dominio no tenga https
OPENCLAW_DASHBOARD_PUBLIC_URL=http://tu-dominio.com
